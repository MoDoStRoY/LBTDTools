export default class CarCertificate
{
    public number: string;
    public date: string;

    constructor(number: string, date: string)
    {
        this.number = number;
        this.date = date;
    }
}