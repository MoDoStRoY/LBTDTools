export default class CarPassport
{
    public number: string;
    public date: string;

    constructor(number: string, date: string)
    {
        this.number = number;
        this.date = date;
    }
}